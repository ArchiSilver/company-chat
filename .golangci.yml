run:
  tests: true

linters:
  enable:
    - govet
    - errcheck
    - gofmt
    - goimports
    - staticcheck
    - ineffassign
    - deadcode
    - gosimple
    - unused

issues:
  exclude-use-default: false
